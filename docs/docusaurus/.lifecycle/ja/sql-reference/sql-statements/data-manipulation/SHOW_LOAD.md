---
displayed_sidebar: "Japanese"
---

# SHOW LOAD

## 説明

データベース内のすべてのロードジョブまたは指定されたロードジョブの情報を表示します。 このステートメントでは、[ブローカーロード](../data-manipulation/BROKER_LOAD.md)、[INSERT](./INSERT.md)、または[Spark Load](../data-manipulation/SPARK_LOAD.md)を使用して作成されたロードジョブのみ表示できます。 また、`curl`コマンドを使用してロードジョブの情報を表示することもできます。 v3.1以降では、`information_schema`データベースの`loads`テーブルから[SELECT](../data-manipulation/SELECT.md)ステートメントを使用してブローカーロードまたはINSERTジョブの結果をクエリすることを推奨します。 詳細については、[HDFSからデータをロード](../../../loading/hdfs_load.md)、[クラウドストレージからデータをロード](../../../loading/cloud_storage_load.md)、[INSERTを使用してデータをロード](../../../loading/InsertInto.md)、および[Apache Spark™を使用したバルクロード](../../../loading/SparkLoad.md)を参照してください。

先行するロード方法に加えて、StarRocksではStream LoadおよびRoutine Loadを使用してデータをロードすることができます。 Stream Loadは同期操作であり、Stream Loadジョブの情報を直接返します。 Routine Loadは非同期操作であり、[SHOW ROUTINE LOAD](../data-manipulation/SHOW_ROUTINE_LOAD.md)ステートメントを使用してRoutine Loadジョブの情報を表示できます。

## 構文

```SQL
SHOW LOAD [ FROM db_name ]
[
   WHERE [ LABEL { = "label_name" | LIKE "label_matcher" } ]
         [ [AND] STATE = { "PENDING" | "ETL" | "LOADING" | "FINISHED" | "CANCELLED" } ]
]
[ ORDER BY field_name [ ASC | DESC ] ]
[ LIMIT { [offset, ] limit | limit OFFSET offset } ]
```

> **注記**
>
> このステートメントに`\G`オプションを追加することで（たとえば `SHOW LOAD WHERE LABEL = "label1"\G;`のように）、通常の水平テーブル形式ではなく、縦に出力することができます。 詳細については、[Example 1](#examples)を参照してください。

## パラメータ

| **パラメータ**                    | **必須** | **説明**                                                     |
| -------------------------------- | -------- | ------------------------------------------------------------ |
| db_name                           | いいえ   | データベース名。 このパラメータが指定されていない場合、デフォルトで現在のデータベースが使用されます。 |
| LABEL = "label_name"              | いいえ   | ロードジョブのラベル。                                      |
| LABEL LIKE "label_matcher"        | いいえ   | このパラメータが指定されている場合、ラベルに`label_matcher`を含むロードジョブの情報が返されます。 |
| AND                               | いいえ   | <ul><li>WHERE句でフィルタ条件を1つだけ指定する場合、このキーワードは指定しないでください。 例：`WHERE STATE = "PENDING"`。</li><li>WHERE句でフィルタ条件を2つまたは3つ指定する場合、このキーワードを指定する必要があります。 例：`WHERE LABEL = "label_name" AND STATE = "PENDING"`。</li></ul> |
| STATE                             | いいえ   | ロードジョブの状態。 ロード方法によって状態が異なります。<ul><li>ブローカーロード<ul><li>`PENDING`：ロードジョブが作成されました。</li><li>`QUEUEING`：ロードジョブがスケジュール待ちのキューにあります。</li><li>`LOADING`：ロードジョブが実行中です。</li><li>`PREPARED`：トランザクションが確定されました。</li><li>`FINISHED`：ロードジョブが成功しました。</li><li>`CANCELLED`：ロードジョブが失敗しました。</li></ul></li><li>Spark Load<ul><li>`PENDING`：StarRocksクラスターがETL関連の構成を準備し、その後ETLジョブをApache Spark™クラスターに提出します。</li><li>`ETL`：SparkクラスターがETLジョブを実行し、その後データを対応するHDFSクラスターに書き込みます。</li><li>`LOADING`：HDFSクラスターのデータがStarRocksクラスターにロードされているため、ロードジョブが実行中です。</li><li>`PREPARED`：トランザクションが確定されました。</li><li>`FINISHED`：ロードジョブが成功しました。</li><li>`CANCELLED`：ロードジョブが失敗しました。</li></ul></li><li>INSERT<ul><li>`FINISHED`：ロードジョブが成功しました。</li><li>`CANCELLED`：ロードジョブが失敗しました。</li></ul></li></ul>`STATE`パラメータが指定されていない場合、デフォルトですべての状態のロードジョブの情報が返されます。 `STATE`パラメータが指定されている場合、指定された状態のロードジョブの情報のみが返されます。 たとえば、`STATE = "PENDING"`は`PENDING`状態のロードジョブの情報を返します。 |
| ORDER BY field_name [ASC \| DESC] | いいえ   | このパラメータが指定されている場合、出力はフィールドに基づいて昇順または降順でソートされます。 次のフィールドがサポートされています：`JobId`、`Label`、`State`、`Progress`、`Type`、`EtlInfo`、`TaskInfo`、`ErrorMsg`、`CreateTime`、`EtlStartTime`、`EtlFinishTime`、`LoadStartTime`、`LoadFinishTime`、`URL`、および`JobDetails`。<ul><li>昇順で出力をソートするには、`ORDER BY field_name ASC`を指定します。</li><li>降順で出力をソートするには、`ORDER BY field_name DESC`を指定します。</li></ul>フィールドとソート順序を指定しない場合、出力はデフォルトで`JobId`の昇順でソートされます。 |
| LIMIT limit                       | いいえ   | 表示が許可されているロードジョブの数。 このパラメータが指定されていない場合、フィルタ条件に一致するすべてのロードジョブの情報が表示されます。 たとえば、`LIMIT 10`を指定すると、フィルタ条件に一致する10個のロードジョブの情報のみが返されます。 |
| OFFSET offset                     | いいえ   | `offset`パラメータはスキップするロードジョブの数を定義します。 たとえば、`OFFSET 5`は最初の5つのロードジョブをスキップして残りを返します。 `offset`パラメータの値はデフォルトで`0`になります。 |

## 出力

```SQL
+-------+-------+-------+----------+------+---------+----------+----------+------------+--------------+---------------+---------------+----------------+-----+------------+
| JobId | Label | State | Progress | Type | Priority | EtlInfo | TaskInfo | ErrorMsg | CreateTime | EtlStartTime | EtlFinishTime | LoadStartTime | LoadFinishTime | URL | JobDetails |
+-------+-------+-------+----------+------+---------+----------+----------+------------+--------------+---------------+---------------+----------------+-----+------------+
```

このステートメントの出力は、ロード方法によって異なります。

| **フィールド** | **ブローカーロード**                                          | **Spark Load**                                               | **INSERT**                                                   |
| -------------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| JobId          | StarRocksがStarRocksクラスター内のロードジョブを識別するために割り当てた一意のID。 | Spark Loadジョブと同じ意味を持ちます。 | INSERTジョブとブローカーロードジョブと同じ意味を持ちます。 |
| Label          | ロードジョブのラベル。 ロードジョブのラベルはデータベース内では一意ですが、異なるデータベース間では重複することがあります。 | Spark Loadジョブと同じ意味を持ちます。 | INSERTジョブとブローカーロードジョブと同じ意味を持ちます。 |
| State          | ロードジョブの状態。<ul><li>`PENDING`：ロードジョブが作成されました。</li><li>`QUEUEING`：ロードジョブがスケジュール待ちのキューにあります。</li><li>`LOADING`：ロードジョブが実行中です。</li><li>`PREPARED`：トランザクションが確定されました。</li><li>`FINISHED`：ロードジョブが成功しました。</li><li>`CANCELLED`：ロードジョブが失敗しました。</li></ul> | ロードジョブの状態。<ul><li>`PENDING`：StarRocksクラスターがETL関連の構成を準備し、その後ETLジョブをApache Spark™クラスターに提出します。</li><li>`ETL`：SparkクラスターがETLジョブを実行し、その後データを対応するHDFSクラスターに書き込みます。</li><li>`LOADING`：HDFSクラスターのデータがStarRocksクラスターにロードされているため、ロードジョブが実行中です。</li><li>`PREPARED`：トランザクションが確定されました。</li><li>`FINISHED`：ロードジョブが成功しました。</li><li>`CANCELLED`：ロードジョブが失敗しました。</li></ul> | ロードジョブの状態。<ul><li>`FINISHED`：ロードジョブが成功しました。</li><li>`CANCELLED`：ロードジョブが失敗しました。</li></ul> |
| 進捗           | ロードジョブの段階。ブローカーロードジョブには、段階を表す `LOAD` のみがあり、0% から 100% までの進捗を示す。ロードジョブが `LOAD` 段階に入ると、`State` パラメータには `LOADING` が返される。ブローカーロードジョブには `ETL` 段階はない。`ETL` パラメータは、スパークロードジョブにのみ有効です。<br />**注釈**<ul><li>`LOAD` 段階の進捗を計算する式：データロードが完了した StarRocks テーブルの数/データをロードする予定の StarRocks テーブルの数 * 100%。</li><li>すべてのデータが StarRocks にロードされると、`LOAD` パラメータには `99%` が返されます。その後、ロードされたデータが StarRocks に反映されると、`LOAD` パラメータには `100%` が返されます。</li><li>`LOAD` 段階の進捗は一定ではないため、データロードが進行中でも `LOAD` パラメータの値が一定期間変化しないことがあります。</li></ul> | ロードジョブの段階。スパークロードジョブには 2 つの段階があります：<ul><li>`ETL`：`ETL` 段階の進捗を示す 0% から 100% の値。</li><li>`LOAD`：`LOAD` 段階の進捗を示す 0% から 100% の値。</li></ul>ロードジョブが `ETL` 段階に入ると、`State` パラメータには `ETL` が返される。ロードジョブが `LOAD` 段階に移行すると、`State` パラメータには `LOADING` が返される。<br />**注釈** ブローカーロードと同じです。 | ロードジョブの段階。INSERTジョブには、ステージを表す `LOAD` のみがあり、0% から 100% までの進捗を示します。ロードジョブが `LOAD` 段階に入ると、`State` パラメータには `LOADING` が返されます。INSERTジョブには `ETL` 段階はありません。したがって、`ETL` パラメータは無効です。**[注釈]**は、ブローカーロードと同じです。 |
| タイプ           | ロードジョブの方法。このパラメータの値は `BROKER` にデフォルトで設定されます。 | ロードジョブの方法。このパラメータの値は `SPARK` にデフォルトで設定されます。 | ロードジョブの方法。このパラメータの値は `INSERT` にデフォルトで設定されます。 |
| 優先度       | ロードジョブの優先度。有効な値：LOWEST、LOW、NORMAL、HIGH、HIGHEST。 | - | - |
| EtlInfo        | ETL 関連のメトリクス。<ul><li>`unselected.rows`: WHERE 句によってフィルタリングされた行数。</li><li>`dpp.abnorm.ALL`: ソーステーブルと StarRocks テーブルの間で、データタイプや列の数などに不一致があるデータ品質の問題によりフィルタリングされた行数。</li><li>`dpp.norm.ALL`: StarRocks クラスタにロードされた行数。</li></ul>上記のメトリクスの合計が生データの総行数です。`max-filter-ratio` パラメータの値を超える不適合データの割合を計算するために次の式を使用できます:`dpp.abnorm.ALL`/(`unselected.rows` + `dpp.abnorm.ALL` + `dpp.norm.ALL`)。 | スパークロードジョブにおいても、ブローカーロードジョブにおいてもフィールドは同じ意味です。 | ETL 関連のメトリック。INSERT ジョブには `ETL` 段階がないため、`NULL` が返されます。 |
| TaskInfo       | ロードジョブを作成する際に指定されたパラメータ。<ul><li>`resource`: このパラメータはスパークロードジョブにのみ有効です。</li><li>`timeout`: ロードジョブが実行を許可される期間。単位：秒。</li><li>`max-filter-ratio`: データ品質の問題によってフィルタリングされた行数の最大割合。</li></ul>詳細については、[BROKER LOAD](../data-manipulation/BROKER_LOAD.md) を参照してください。 | ロードジョブを作成する際に指定されたパラメータ。<ul><li>`resource`: リソース名。</li><li>`timeout`: ロードジョブが実行を許可される期間。単位：秒。</li><li>`max-filter-ratio`: データ品質の問題によってフィルタリングされた行数の最大割合。</li></ul>詳細については、[SPARK LOAD](../data-manipulation/SPARK_LOAD.md) を参照してください。 | ロードジョブを作成する際に指定されたパラメータ。<ul><li>`resource`: このパラメータはスパークロードジョブにのみ有効です。</li><li>`timeout`: ロードジョブが実行を許可される期間。単位：秒。</li><li>`max-filter-ratio`: データ品質の問題によってフィルタリングされた行数の最大割合。</li></ul>詳細については、[INSERT](./INSERT.md) を参照してください。 |
| ErrorMsg       | ロードジョブが失敗したときに返されるエラーメッセージ。ロードジョブのステートが `PENDING`、`LOADING`、または `FINISHED` の場合、`ErrorMsg` フィールドには `NULL` が返されます。ロードジョブのステートが `CANCELLED` の場合、`ErrorMsg` フィールドに返される値は `type` と `msg` の 2 部分から成ります。<ul><li>`type` 部分には、以下の値のいずれかが含まれます:<ul><li>`USER_CANCEL`: ロードジョブは手動でキャンセルされました。</li><li>`ETL_SUBMIT_FAIL`: StarRocks が Spark に ETL ジョブをサブミットできませんでした。</li><li>`ETL-RUN-FAIL`: Spark が ETL ジョブを実行できませんでした。</li><li>`ETL-QUALITY-UNSATISFIED`: データの不適合率が `max-filter-ratio` パラメータの値を超えたため、ロードジョブが失敗しました。</li><li>`LOAD-RUN-FAIL`: ロードジョブが `LOAD` ステージで失敗しました。</li><li>`TIMEOUT`: 指定されたタイムアウト期間内にロードジョブが完了しませんでした。</li><li>`UNKNOWN`: 未知のエラーによりロードジョブが失敗しました。</li></ul></li><li>`msg` 部分には、ロードの失敗の詳細原因が提供されます。</li></ul> | ロードジョブが失敗したときに返されるエラーメッセージ。ロードジョブのステートが `PENDING`、`LOADING`、または `FINISHED` の場合、`ErrorMsg` フィールドには `NULL` が返されます。ロードジョブのステートが `CANCELLED` の場合、`ErrorMsg` フィールドに返される値は `type` と `msg` の 2 部分から成ります。<ul><li>`type` 部分には、以下の値のいずれかが含まれます:<ul><li>`USER_CANCEL`: ロードジョブは手動でキャンセルされました。</li><li>`ETL_SUBMIT_FAIL`: StarRocks が Spark に ETL ジョブをサブミットできませんでした。</li><li>`ETL-RUN-FAIL`: Spark が ETL ジョブを実行できませんでした。</li><li>`ETL-QUALITY-UNSATISFIED`: データの不適合率が `max-filter-ratio` パラメータの値を超えたため、ロードジョブが失敗しました。</li><li>`LOAD-RUN-FAIL`: ロードジョブが `LOAD` ステージで失敗しました。</li><li>`TIMEOUT`: 指定されたタイムアウト期間内にロードジョブが完了しませんでした。</li><li>`UNKNOWN`: 未知のエラーによりロードジョブが失敗しました。</li></ul></li><li>`msg` 部分には、ロードの失敗の詳細原因が提供されます。</li></ul> | ロードジョブが失敗したときに返されるエラーメッセージ。ロードジョブのステートが `FINISHED` の場合、`ErrorMsg` フィールドには `NULL` が返されます。ロードジョブのステートが `CANCELLED` の場合、`ErrorMsg` フィールドに返される値は `type` と `msg` の 2 部分から成ります。<ul><li>`type` 部分には、以下の値のいずれかが含まれます:<ul><li>`USER_CANCEL`: ロードジョブは手動でキャンセルされました。</li><li>`ETL_SUBMIT_FAIL`: ロードジョブがサブミットできませんでした。</li><li>`ETL_RUN_FAIL`: ロードジョブは実行できませんでした。</li><li>`ETL_QUALITY_UNSATISFIED`: 生データの品質に問題があるため、ロードジョブが失敗しました。</li><li>`LOAD-RUN-FAIL`: ロードジョブが `LOAD` ステージで失敗しました。</li><li>`TIMEOUT`: 指定されたタイムアウト期間内にロードジョブが完了しませんでした。</li><li>`UNKNOWN`: 未知のエラーによりロードジョブが失敗しました。</li><li>`TXN_UNKNOWN`: ロードジョブのトランザクションの状態が不明なため失敗しました。</li></ul></li><li>`msg` 部分には、ロードの失敗の詳細原因が提供されます。</li></ul> |
| CreateTime     | ロードジョブが作成された時刻。                  | ロードジョブにおいても、ブローカーロードジョブにおいてもフィールドは同じ意味です。 | ロードジョブにおいても、ブローカーロードジョブにおいてもフィールドは同じ意味です。 |
| EtlStartTime   | ブローカーロードジョブには `ETL` ステージがありません。そのため、このフィールドの値は `LoadStartTime` フィールドの値と同じです。   | `ETL` ステージの開始時刻。                                       | INSERTジョブには `ETL` ステージがありません。そのため、このフィールドの値は `LoadStartTime` フィールドの値と同じです。   |
| EtlFinishTime  | ブローカーロードジョブには `ETL` ステージがありません。そのため、このフィールドの値は `LoadStartTime` フィールドの値と同じです。   | `ETL` ステージの終了時刻。                                       | INSERTジョブには `ETL` ステージがありません。そのため、このフィールドの値は `LoadStartTime` フィールドの値と同じです。   |
| LoadStartTime  | `LOAD` ステージの開始時刻。                                        | このフィールドは、ブローカーロードジョブと同じ意味を持ちます。Sparkロードジョブにも。                                 | このフィールドは、ブローカーロードジョブと同じ意味を持ちます。INSERTジョブにも。                                       |
| LoadFinishTime | ロードジョブの終了時刻。                                           | このフィールドは、ブローカーロードジョブと同じ意味を持ちます。Sparkロードジョブにも。                                 | このフィールドは、ブローカーロードジョブと同じ意味を持ちます。INSERTジョブにも。                                       |
| URL            | ロードジョブで検出された非適格データにアクセスするために使用されるURL。`curl` コマンドまたは `wget` コマンドを使用して、URLにアクセスして非適格データを取得できます。非適格データが検出されない場合、`NULL` が返されます。 | このフィールドは、ブローカーロードジョブと同じ意味を持ちます。Sparkロードジョブにも。                                 | このフィールドは、ブローカーロードジョブと同じ意味を持ちます。INSERTジョブにも。                                       |
| JobDetails     | ロードジョブに関連するその他の情報。<ul><li>`Unfinished backends`: データロードが完了していないBEのID。</li><li>`ScannedRows`: StarRocksにロードされた行の総数およびフィルタリングされた行の数。</li><li>`TaskNumber`: ロードジョブは1つ以上のタスクに分割される場合があり、これは同時に実行されるロードタスクの数を示します。</li><li>`All backends`: データロードを実行しているBEのID。</li><li>`FileNumber`: 元のデータファイルの数。</li><li>`FileSize`: 元のデータファイルのデータ容量。単位: バイト。</li></ul> | このフィールドは、ブローカーロードジョブと同じ意味を持ちます。Sparkロードジョブにも。                                 | このフィールドは、ブローカーロードジョブと同じ意味を持ちます。INSERTジョブにも。                                       |

## 使用上の注意

- `LOAD` ジョブの `LoadFinishTime` から 3 日間、 `SHOW LOAD` ステートメントによって返される情報は有効です。3 日経過すると情報を表示できなくなります。デフォルトの有効期間を変更するには、`label_keep_max_second` パラメータを使用できます。

    ```SQL
    ADMIN SET FRONTEND CONFIG ("label_keep_max_second" = "value");
    ```

- `LoadStartTime` フィールドの値が長い間 `N/A` の場合、ロードジョブが大量に積み重なっていることを意味します。ロードジョブの作成頻度を減らすことをお勧めします。
- ロードジョブによって消費される合計時間期間 = `LoadFinishTime` - `CreateTime`。
- ロードジョブが `LOAD` ステージで消費した合計時間期間 = `LoadFinishTime` - `LoadStartTime`。

## 例

例 1: 現在のデータベースに含まれるすべてのロードジョブを縦に表示します。

```plaintext
SHOW LOAD\G;
*************************** 1. row ***************************
         JobId: 976331
         Label: duplicate_table_with_null
         State: FINISHED
      Progress: ETL:100%; LOAD:100%
          Type: BROKER
      Priority: NORMAL
       EtlInfo: unselected.rows=0; dpp.abnorm.ALL=0; dpp.norm.ALL=65546
      TaskInfo: resource:N/A; timeout(s):300; max_filter_ratio:0.0
      ErrorMsg: NULL
    CreateTime: 2022-10-17 19:35:00
  EtlStartTime: 2022-10-17 19:35:04
 EtlFinishTime: 2022-10-17 19:35:04
 LoadStartTime: 2022-10-17 19:35:04
LoadFinishTime: 2022-10-17 19:35:06
           URL: NULL
    JobDetails: {"Unfinished backends":{"b90a703c-6e5a-4fcb-a8e1-94eca5be0b8f":[]},"ScannedRows":65546,"TaskNumber":1,"All backends":{"b90a703c-6e5a-4fcb-a8e1-94eca5be0b8f":[10004]},"FileNumber":1,"FileSize":548622}
```

例 2: ラベルに文字列 `null` を含む2つのロードジョブを表示します。

```plaintext
SHOW LOAD 
WHERE LABEL LIKE "null" 
LIMIT 2;

+-------+---------------------------+----------+---------------------+--------+---------------------------------------------------------+---------------------------------------------------------------------------------------------------------+----------+---------------------+---------------------+---------------------+---------------------+---------------------+--------------------------------------------------------------------------------+---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| JobId | Label                     | State    | Progress            | Type   | EtlInfo                                                 | TaskInfo                                                                                                | ErrorMsg | CreateTime          | EtlStartTime        | EtlFinishTime       | LoadStartTime       | LoadFinishTime      | URL                                                                            | JobDetails                                                                                                                                                                                              |
+-------+---------------------------+----------+---------------------+--------+---------------------------------------------------------+---------------------------------------------------------------------------------------------------------+----------+---------------------+---------------------+---------------------+---------------------+---------------------+--------------------------------------------------------------------------------+---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| 10082 | duplicate_table_with_null | FINISHED | ETL:100%; LOAD:100% | BROKER | unselected.rows=0; dpp.abnorm.ALL=0; dpp.norm.ALL=65546 | resource:N/A; timeout(s):300; max_filter_ratio:0.0                                                      | NULL     | 2022-08-02 14:53:27 | 2022-08-02 14:53:30 | 2022-08-02 14:53:30 | 2022-08-02 14:53:30 | 2022-08-02 14:53:31 | NULL                                                                           | {"Unfinished backends":{"4393c992-5da1-4e9f-8b03-895dc0c96dbc":[]},"ScannedRows":65546,"TaskNumber":1,"All backends":{"4393c992-5da1-4e9f-8b03-895dc0c96dbc":[10002]},"FileNumber":1,"FileSize":548622} |
| 10103 | unique_table_with_null    | FINISHED | ETL:100%; LOAD:100% | SPARK  | unselected.rows=0; dpp.abnorm.ALL=0; dpp.norm.ALL=65546 | resource:test_spark_resource_07af473a_1230_11ed_b483_00163e0e550b; timeout(s):300; max_filter_ratio:0.0 | NULL     | 2022-08-02 14:56:06 | 2022-08-02 14:56:19 | 2022-08-02 14:56:41 | 2022-08-02 14:56:41 | 2022-08-02 14:56:44 | http://emr-header-1.cluster-49091:20888/proxy/application_1655710334658_26391/ | {"Unfinished backends":{"00000000-0000-0000-0000-000000000000":[]},"ScannedRows":65546,"TaskNumber":1,"All backends":{"00000000-0000-0000-0000-000000000000":[-1]},"FileNumber":1,"FileSize":8790855}   |
+-------+---------------------------+----------+---------------------+--------+---------------------------------------------------------+---------------------------------------------------------------------------------------------------------+----------+---------------------+---------------------+---------------------+---------------------+---------------------+--------------------------------------------------------------------------------+---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

例 3: `example_db` に含まれるラベルに文字列 `table` を含むロードジョブを、 `LoadStartTime` フィールドの降順で表示します。

```plaintext
SHOW LOAD FROM example_db 
WHERE LABEL Like "table" 
ORDER BY LoadStartTime DESC;

+-------+---------------------------+----------+---------------------+--------+---------------------------------------------------------+---------------------------------------------------------------------------------------------------------+----------+---------------------+---------------------+---------------------+---------------------+---------------------+--------------------------------------------------------------------------------+---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
```plaintext
      +-------+-----------------------------------------+----------+---------------------+--------+-------------------------------------------------------------+---------------------------------------------------------------------------------------------------------+----------+---------------------+---------------------+---------------------+---------------------+---------------------+--------------------------------------------------------------------------------+---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| 10103 | unique_table_with_null                      | FINISHED | ETL:100%; LOAD:100% | SPARK  | unselected.rows=0; dpp.abnorm.ALL=0; dpp.norm.ALL=65546 | resource:test_spark_resource_07af473a_1230_11ed_b483_00163e0e550b; timeout(s):300; max_filter_ratio:0.0 | NULL     | 2022-08-02 14:56:06 | 2022-08-02 14:56:19 | 2022-08-02 14:56:41 | 2022-08-02 14:56:41 | 2022-08-02 14:56:44 | http://emr-header-1.cluster-49091:20888/proxy/application_1655710334658_26391/ | {"Unfinished backends":{"00000000-0000-0000-0000-000000000000":[]},"ScannedRows":65546,"TaskNumber":1,"All backends":{"00000000-0000-0000-0000-000000000000":[-1]},"FileNumber":1,"FileSize":8790855} |
| 10120 | insert_3a57b595-1230-11ed-b075-00163e14c85e | FINISHED | ETL:100%; LOAD:100% | INSERT | NULL                                                        | resource:N/A; timeout(s):3600; max_filter_ratio:0.0                                         | NULL     | 2022-08-02 14:56:26 | 2022-08-02 14:56:26 | 2022-08-02 14:56:26 | 2022-08-02 14:56:26 | 2022-08-02 14:56:26 |                                                                                | {"Unfinished backends":{},"ScannedRows":0,"TaskNumber":0,"All backends":{},"FileNumber":0,"FileSize":0}                                                                                               |
```